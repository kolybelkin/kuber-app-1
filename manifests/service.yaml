apiVersion: v1
kind: Service
metadata:
  name: kolybelkin-app-service
  namespace: kolybelkin
spec:
  selector:
    app: kolybelkin-app
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080